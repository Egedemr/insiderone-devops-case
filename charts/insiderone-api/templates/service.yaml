apiVersion: v1
kind: Service
metadata:
  name: {{ include "insiderone-api.fullname" . }}
  labels:
    {{- include "insiderone-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "insiderone-api.selectorLabels" . | nindent 4 }}
